import { createTheme } from "@mui/material";


export const theme = createTheme({
    palette: {
        primary: {
            main: '#000',
            light: '#000'
        },
        secondary: {
            main: '#000',
            light: '#000',
        },
        otherColor: {
            main: '#000',
            light: '#000'
        }
    }
})